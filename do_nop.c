#include "monty.h"

/**
 * do_nop - opcode does not do anything
 * @n_head: pointer pointing to the first node of list
 * @line_number: line number
*/
void do_nop(stack_t **n_head, unsigned int line_number)
{
	(void)n_head;
	(void)line_number;
}
